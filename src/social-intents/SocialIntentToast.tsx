"use client";

export function SocialIntentToast() {
  return (
    <div className="social-intent-toast" role="status" aria-live="polite">
      Only one plan at a time — choosing another set will move your ‘I’m going’ there.
    </div>
  );
}
